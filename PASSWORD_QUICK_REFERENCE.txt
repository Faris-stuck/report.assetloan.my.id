# 🔑 Password Plaintext System - Quick Reference

## Default Password untuk Semua User
```
123456
```

## Files Modified (5 files)
1. ✅ `api/auth/login.php` - Login verification
2. ✅ `api/auth/register.php` - User registration
3. ✅ `api/auth/forgot-password.php` - Password reset
4. ✅ `api/user/create.php` - Admin create user
5. ✅ `api/user/change_password.php` - User change password

## New File Created (1 file)
- ✅ `api/reset_password_plaintext.php` - Reset all passwords to default

## Key Changes

### Compare Function Changed
```php
// SEBELUM (hashed)
if (!password_verify($password, $stored)) { ... }

// SESUDAH (plaintext)
if ($password !== $stored) { ... }
```

### Hash Removed
```php
// SEBELUM
$hashed = password_hash($password, PASSWORD_BCRYPT);
INSERT INTO users ... VALUES ($hashed)

// SESUDAH
INSERT INTO users ... VALUES ($password)
```

## Testing Login
```
Email: admin@komatsu.co.id
Password: 123456
```

## Reset All Passwords
### Via Browser
```
http://localhost/PROJECT/api/reset_password_plaintext.php
```

### Via CLI
```bash
php api/reset_password_plaintext.php
```

### Response
```json
{
  "status": true,
  "message": "Password reset berhasil",
  "details": {
    "default_password": "123456",
    "users_updated": 5
  }
}
```

## Verification Commands
```bash
# Check no hash functions remain
grep -r "password_hash\|password_verify" api/

# Check database passwords
mysql -u root peminjaman -e "SELECT nama, password FROM users;"
```

## ⚠️ Important
- Development/Testing ONLY
- No production use
- No real user data
- Plaintext storage in database

## Flowchart
```
User Input Password → Plaintext Comparison
                   ↓
            $_POST['password'] (plaintext)
                   ↓
            Compare with DB password (plaintext)
                   ↓
         Login Success/Fail
```

---
**Status:** ✅ Active - Plaintext Password System
**Database:** Updated - All passwords = 123456
**Last Updated:** 2026-02-23
